# @package model_config
_target_: dna2vec_sft.sft_config_schema.SFTModelConfigSchema

# Pre-trained encoder path (optional)
pretrained_encoder_path: "/mnt/SSD7/yigit/dnarde/regularization_0.0_pool_1020mean_checkpoint.pt"

# Model architecture parameters - LARGE variant
embedding_dim: 1020
dim_feedforward: 2048  # Larger feedforward
num_heads: 16          # More attention heads
num_layers: 8          # More layers
dropout: 0.1
activation: "gelu"
max_position_embeddings: 1024

# MLP head configuration - LARGE variant
mlp_config:
  _target_: dna2vec_sft.sft_config_schema.MLPConfigSchema
  input_dim: 1020
  hidden_dim: 1024     # Larger hidden dimension
  output_dim: 1020
  dropout: 0.15        # Slightly more dropout
  activation: "gelu"   # Different activation
  num_layers: 3        # More layers

# Training options
freeze_encoder: false 